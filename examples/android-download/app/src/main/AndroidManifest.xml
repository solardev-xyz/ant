<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      INTERNET is the only permission the smoke test needs. We don't
      use ACCESS_NETWORK_STATE (no online/offline detection),
      ACCESS_WIFI_STATE (no Wi-Fi probe), or FOREGROUND_SERVICE (the
      proper M2 Phase 4 artefact will add this once the foreground
      service for active downloads / uploads lands — PLAN.md § 10).
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AntDownloadApp"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AntDownload"
        android:allowBackup="false"
        tools:targetApi="34"
        xmlns:tools="http://schemas.android.com/tools">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AntDownload">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
